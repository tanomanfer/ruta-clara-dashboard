# ESTADO_PROYECTO

> Este es el archivo canónico de estado para todo "chofer-rentable" (incluye `ruta-clara-android`, que solo tiene un puntero a este archivo).

## Última actualización
2026-08-26 — ✅ Mapa táctil/ampliable y permanencia validados con una oferta real en el Redmi; 11 pruebas pasan.

## ⭐ RESUMEN RÁPIDO PARA RETOMAR (leer esto primero)
- La app ANDA: semáforo automático sobre Uber real, en el celu de Fernando (Redmi Note 8 Pro). Cálculo $/hora y $/km verificado correcto con muchos viajes reales.
- Proyecto Android: ~/proyectos/chofer-rentable/ruta-clara-dashboard (carpeta app/). Kotlin+XML.
- APK generado (DEBUG): ruta-clara-dashboard/app/build/outputs/apk/debug/app-debug.apk. Fernando ya hizo una copia linda en ~/Escritorio/RutaClara.apk (58 MB) para compartir.
- Material de difusión en ~/proyectos/chofer-rentable/marketing/: flyer_rutaclara.png, guia_instalacion.png, icono_rutaclara.png, mensaje_whatsapp.txt.
- Cómo compartir: por WhatsApp (Fernando lo usa en la compu). Orden: flyer → mensaje_whatsapp.txt → RutaClara.apk → guia_instalacion.png. WhatsApp muestra el APK como cuadradito gris genérico (NO se puede cambiar, es de WhatsApp); la confianza se genera con el flyer + el mensaje de Fernando. La app SÍ muestra el ícono semáforo una vez instalada.
- Compilar: `cd ~/proyectos/chofer-rentable/ruta-clara-dashboard && env JAVA_HOME=$HOME/android-studio/jbr ANDROID_HOME=$HOME/Android/Sdk ./gradlew assembleDebug`
- Instalar en el Redmi (MIUI bloquea adb install): `adb push app-debug.apk /sdcard/Download/rutaclara.apk` y instalar a mano (Descargas→tocar→"Analizar app"→Instalar). adb WiFi: puerto rota, verlo en Depuración inalámbrica, `adb connect 192.168.0.18:PUERTO`. scrcpy para ver el celu en la compu: `scrcpy -s 192.168.0.18:PUERTO`.
- Permiso de CAPTURA sobrevive al bloqueo de pantalla; se pierde al REINICIAR el celu (reactivar con el botón verde). Accesibilidad se mantiene activada.
- PRÓXIMO GRANDE: el MAPA de zonas de seguridad (diferencial PAGO). El destino ya se lee en cada viaje → es el insumo. No empezado.

## Para otra IA que retome esto (Antigravity u otra)
Ruta Clara: app para choferes de Uber/DiDi en Argentina que calcula rentabilidad por viaje ($/km o $/hora, criterio elegido por el chofer) y muestra un semáforo verde/ámbar/rojo, con mapa de zonas de riesgo como diferencial. Stack: TanStack Start + React Query + Tailwind + shadcn/ui + Supabase (Postgres + Auth + RLS). Repo: tanomanfer/ruta-clara-dashboard. 80% del trabajo mecánico va a Gemini (Antigravity); Claude Code (Opus) se reserva para schema/RLS/auth. Etapa: dogfooding con Tano y un amigo. Todavía no hay pipeline real de datos (Accessibility Service Android, Fase 4, no empezada) — todo lo cargado hoy es manual.

## Qué se hizo (acumulado)
- Auth real, landing pública con leads.
- Módulo 1: objetivos reales en /setup + selector de criterio ($/km o $/hora, columna criterio_evaluacion).
- Módulo 2: /home real (ganancia y promedios de hoy desde Supabase).
- Módulo 3: /history real.
- Módulo 4: /registrar-viaje, formulario manual para cargar viajes de prueba (calcula rentabilidad y estado según criterio; amarillo = 90% del objetivo).
- Fase 2: mapa automático (Leaflet) en /trip-mobile y /trip-tablet, visible en verde/ámbar, oculto en rojo.
- Fase 3: tabla zonas (motivo: inseguridad/calle_rota/otro, compartido boolean) + pantalla /zonas con mapa interactivo (clic mueve marcador candidato, doble clic fija y guarda). Zonas reales ya conectadas al mapa de /trip-mobile y /trip-tablet.

- Fase 4 (arranque, sin commitear todavía): servicio `ScreenCaptureService.kt` con `MediaProjection` + ML Kit (OCR on-device) que captura la pantalla y lee todo el texto visible con su posición (x,y). En `MainActivity` hay botones de prueba manual ("Activar lectura de pantalla" / "Probar captura ahora") que solo muestran las líneas leídas — todavía no interpretan cuál es tarifa, distancia o destino.

## Qué falta / próximos pasos
- Botón de recentrar ubicación en /zonas.
- Destino en trip-mobile/trip-tablet sigue siendo de ejemplo (Obelisco) hasta Fase 4.
- Fase 4 (Android nativo): en curso. Falta interpretar las líneas de OCR (identificar tarifa/distancia/destino dentro de la tarjeta de viaje) y automatizar la captura (hoy es manual con botón de prueba).

## Decisiones tomadas (no repreguntar)
- Sin cálculo de combustible en el MVP.
- Un solo criterio de evaluación por chofer, nunca los dos a la vez.
- Mapa: Google Maps para navegación real (link externo), OpenStreetMap/Leaflet para vista embebida (gratis, sin API key).
- Nunca mencionar Uber/DiDi/Waze/Accessibility Service en contenido público.

## Notas / bloqueos
- 2026-08-24: Cowork (Claude en claude.ai) quedó conectado a la carpeta ~/proyectos vía el puente de la app de escritorio. De ahora en más lee y actualiza este archivo solo, igual que Codex/Antigravity. Fernando siempre arranca con Claude; Codex/Antigravity son refuerzo para cuando se acaban los tokens.
- 2026-08-24: Sesión de instalación en celular real (Redmi Note 8 Pro, MIUI). Se compiló e instaló la versión `ruta-clara-android` (solo semáforo, view-IDs de MockUber) por adb WiFi — sirvió para verificar que compila e instala en teléfono físico. MockUber se instaló a mano (adb push a /sdcard/Download + instalar desde el Administrador de archivos) porque MIUI bloquea `adb install` con INSTALL_FAILED_USER_RESTRICTED. Conexión adb WiFi: emparejar una vez, después `adb connect 192.168.0.18:PUERTO` (el puerto rota, verlo en Ajustes→Opciones de desarrollador→Depuración inalámbrica). Con varios devices listados usar `adb -s IP:PUERTO`.
- 2026-08-24: DECISIÓN DE RUMBO — se va directo a Uber real por la vía OCR (ScreenCaptureService + ML Kit), NO por view-IDs (Uber los ofusca/cambia). La carpeta de trabajo pasa a ser `ruta-clara-dashboard/app` (ahí vive ScreenCaptureService.kt), no `ruta-clara-android`. Bug conocido a no repetir: Activity.RESULT_OK vale -1; usar Int.MIN_VALUE como centinela en getIntExtra.
- 2026-08-24: DECISIÓN DE PRODUCTO — la rentabilidad se calcula SOLO sobre el viaje ("Viaje: X min (Y km)"), NO se cuenta la ida a buscar al pasajero ("A X min (Y km)").
- 2026-08-24: PASO 4 HECHO (el "cerebro"). Se creó `InterpreteViaje.kt` (nuevo): `object InterpreteViaje` con `interpretar(List<LineaTexto>): Viaje?` (parsea tarifa/km/min/destino de la tarjeta de Uber AR) y `evaluar(Viaje, criterio, objetivo): Evaluacion` (color verde/ámbar/rojo). Data classes: Viaje, Evaluacion, enum ColorSemaforo. Formato Uber AR verificado con 2 capturas reales (Claude las probó en Python antes de escribir el Kotlin): tarifa "ARS4,671" (coma=miles→4671), viaje "Viaje: 15 min (5.0 km)" (punto=decimal), destino=línea siguiente a "Viaje:". Se cableó `MainActivity.kt`: el botón "Probar captura" ahora interpreta y muestra 🟢/🟡/🔴 + valor + tarifa/km/min/destino, en vez de solo listar líneas.
- 2026-08-24: PENDIENTE INMEDIATO — compilar `ruta-clara-dashboard` (assembleDebug), instalar en el Redmi Note 8 Pro y probar con la tarjeta de Uber real en pantalla apretando "Probar captura". Instalación en ese celu: MIUI bloquea `adb install` (INSTALL_FAILED_USER_RESTRICTED) → usar `adb push` del APK a /sdcard/Download y instalar a mano desde el Administrador de archivos (tocar "Analizar app"). adb WiFi: `adb connect 192.168.0.18:PUERTO` (puerto rota, verlo en Depuración inalámbrica); con 2 devices listados usar `adb -s IP:PUERTO`.
- 2026-08-24: DETECCIÓN AUTOMÁTICA HECHA. Se reescribió `RutaClaraAccessibilityService.kt`: ya NO lee por view-IDs de MockUber. Ahora vigila la pantalla; cuando el paquete en foco es Uber (com.ubercab.driver o contiene "uber") y detecta en los nodos de accesibilidad el texto "Viaje:" + "ARS", dispara ScreenCaptureService.capturarYLeer → InterpreteViaje.interpretar → InterpreteViaje.evaluar → mostrarSemaforo (vista flotante), todo SOLO. Anti-rebote 1500ms + huella por (pesos|km|min) para no repetir el mismo viaje. Semáforo por ahora arriba a la derecha (TOP|END, y=120); falta ajustar para no tapar tarifa/botón Aceptar. El botón manual "Probar captura" de MainActivity queda como herramienta de diagnóstico.
- 2026-08-24: DECISIÓN sobre RIESGO/UBER — Fernando prueba con su cuenta real, con cuidado. Reglas fijas del producto para minimizar riesgo de suspensión: (1) NUNCA automatizar (no tocar botones de Uber, no auto-aceptar/rechazar; la app solo MUESTRA un color, el chofer decide y toca con su dedo); (2) el semáforo NO debe tapar la tarifa ni el botón Aceptar; (3) se cierra solo a los 8s. La app solo LEE la pantalla (accesibilidad + captura), no se comunica con los servidores de Uber. Riesgo evaluado como bajo pero no cero; Fernando lo acepta.
- 2026-08-24: PENDIENTE INMEDIATO — compilar dashboard, reinstalar en el Redmi (adb push + instalar a mano por MIUI), y probar en la calle: activar objetivo + activar lectura de pantalla + activar el servicio de accesibilidad "RutaClara", y con un viaje real de Uber ver si el semáforo aparece SOLO. Si no dispara, revisar el nombre de paquete real de la app de Uber del chofer (el filtro asume com.ubercab.driver). Para diagnosticar, leer Logcat: `adb logcat -d -s "RutaClaraService:*" "ScreenCaptureService:*"`.
- 2026-08-24: ✅✅✅ FUNCIONA CON UBER REAL. Probado en el Redmi Note 8 Pro con viajes reales entrando. El semáforo flotante apareció SOLO encima de Uber: viaje UberX Exclusivo ARS6,757 / Viaje 23 min (7.9 km) → 🟢 "$17626/hora CONVIENE" (6757/(23/60)=17626 ✓). Circuito completo andando: detecta Uber (com.ubercab.driver) → OCR lee la pantalla → InterpreteViaje saca tarifa/km/min → evalúa → muestra semáforo flotante. Sin tocar nada.
- 2026-08-24: BUG ENCONTRADO Y RESUELTO que bloqueaba todo: `res/xml/accessibility_service_config.xml` tenía `android:packageNames="com.tanomanfer.mockuber"` → el servicio SOLO escuchaba MockUber, por eso nunca reaccionaba con Uber. Se quitó esa línea (ahora escucha todas las apps). Diagnóstico clave: `adb shell dumpsys accessibility` mostró el servicio Bound/Enabled pero sin eventos; el log con `Log.d` reveló que el filtro de packageNames era la causa. Herramienta usada: scrcpy (`sudo pacman -S scrcpy`, `scrcpy -s IP:PUERTO`) para ver el celu en la compu.
- 2026-08-24: NOTAS del OCR con Uber AR real (para afinar InterpreteViaje): la tarifa viene como "ARS 6,757" (CON espacio después de ARS; el código ya lo contempla). El OCR parte el texto en líneas sueltas y a veces desordenadas ("San Antonio de" / "Padua" en líneas separadas) → la lógica de "destino = línea siguiente a Viaje:" puede fallar a veces; conviene hacerla más robusta. En pantalla de "Buscando solicitud" (sin viaje) el OCR lee "ARS 0.00" y "Buscando solicitud de viaje" → InterpreteViaje correctamente NO muestra semáforo.
- 2026-08-24: POSICIÓN DEL SEMÁFORO AJUSTADA (en RutaClaraAccessibilityService.kt, función mostrarSemaforo): pasó de TOP|END x=20 y=120 a TOP|CENTER_HORIZONTAL con margen superior proporcional (~6% del alto de pantalla), para no tapar el botón Aceptar (que Uber pone abajo) y verse bien tanto en celular como en tablet. FALTA: Fernando compila/reinstala y valida con un viaje real que quede bien ubicado; y probar en su TABLET (quedó pendiente conectarla por adb WiFi — misma mecánica que el celu pero la tablet tiene su propia IP; Fernando la usa en formato tablet y quiere ver cómo se ve el semáforo ahí).
- 2026-08-24: CÓMO TRABAJA FERNANDO CON LAS IAs — hoy trabajó solo con Claude/Cowork (fuente principal). Codex y Antigravity son el refuerzo para cuando se acaban los tokens de Claude. Al arrancar con Codex conviene pedirle explícitamente "leé el ESTADO_PROYECTO.md antes de empezar" (la regla está en AGENTS.md pero los modelos a veces la saltean). Claude actualiza este archivo en cada avance, no al final.
- 2026-08-24: VERIFICACIÓN DEL CÁLCULO (Fernando pasó 3 capturas reales, criterio=hora). El cálculo $/hora es CORRECTO: (1) ARS9.785 / 26min → $22.580/h 🟢 ✓; (3) ARS6.291 / 25min → $15.098/h 🟢 ✓. La matemática (pesos/(min/60)) está bien.
- 2026-08-24: BUG DETECTADO (sincronización semáforo↔tarjeta). En la captura 2 el semáforo mostraba 🟡 $14.640/h OJO PERO la tarjeta visible era ARS9.785/26min (que da $22.580, verde). O sea el semáforo era de un viaje ANTERIOR que quedó en pantalla (dura 8s) mientras abajo ya había aparecido una tarjeta nueva. Riesgo real: el chofer rechaza un viaje bueno por ver un color viejo. A CORREGIR: cuando entra un viaje nuevo (o cambia la tarjeta), borrar/actualizar el semáforo inmediatamente; considerar bajar el tiempo de display o refrescar al detectar tarjeta nueva; revisar la lógica de "ultimaHuellaViaje" para que no bloquee mostrar el viaje nuevo cuando el anterior sigue en pantalla.
- 2026-08-25: ✅ BUG DEL SEMÁFORO PEGADO ARREGLADO (RutaClaraAccessibilityService.kt). Cambios: (1) si el OCR NO encuentra viaje (tarjeta desapareció), se borra el semáforo al toque con quitarSemaforo() y se limpia ultimaHuellaViaje=null → nunca queda un color viejo sin su tarjeta; (2) la huella solo bloquea si es el mismo viaje Y floatingView!=null (si el semáforo ya se cerró, el mismo viaje se vuelve a mostrar); (3) el timer de cierre ahora es un Runnable (runnableQuitar) que se cancela con handler.removeCallbacks al mostrar un semáforo nuevo, así el timer viejo no borra el cartel nuevo; (4) INTERVALO_CAPTURA_MS bajado a 1000ms para reaccionar más rápido.
- 2026-08-25: ✅ VALIDADO en celu real (scrcpy). Viaje ARS3.400 / 8min, objetivo $15.000/h → 🟢 $25.500/h CONVIENE (3400*60/8=25500 ✓). Posición del semáforo TOP|CENTER_HORIZONTAL quedó bien: arriba, sin tapar el botón "Viaje disponible"/"Aceptar". La accesibilidad NO se apaga al reinstalar/bloquear (se mantiene activada). El permiso de CAPTURA (MediaProjection) SÍ sobrevive al bloqueo de pantalla — confirmado por Fernando (probó con el celu bloqueado un buen rato y seguía andando). Lo que se cortaba en las pruebas era scrcpy (herramienta de la compu), NO la captura real. => NO hace falta reactivar captura tras bloquear; sí tras reiniciar el celu.
- 2026-08-25: FALTA VERIFICAR aún (poca demanda de viajes al momento de probar): confirmar en vivo que cuando un viaje se va, el semáforo se borra solo, y que el siguiente viaje muestra SU color (no el anterior). El código ya lo contempla; falta la confirmación visual con 2 viajes seguidos.
- 2026-08-25: MEJORA UI "arranque fácil" HECHA (activity_main.xml + MainActivity.kt). Ahora al abrir la app se ve arriba de todo un cartel grande de estado: 🔴 APAGADO (fondo rojo) o 🟢 LISTO PARA TRABAJAR (fondo verde), con un botón grande verde "ACTIVAR PARA TRABAJAR". La config del objetivo quedó abajo. Se quitó el "(prueba)". La función actualizarPanelEstado() pinta el cartel según ScreenCaptureService.estaListo(), y se refresca en onResume. ✅ COMPILADO E INSTALADO Y VALIDADO 25/08: se ve perfecto — estado APAGADO (cartel rojo + botón verde "ACTIVAR PARA TRABAJAR") y estado LISTO (cartel verde + "YA ESTÁ ACTIVO"). UI clara para el arranque diario.
- 2026-08-25: ✅ BOTÓN PRENDER/APAGAR (interruptor) HECHO Y VALIDADO. El botón grande de MainActivity ahora es toggle: si estaListo()→APAGAR (rojo, llama ScreenCaptureService.detener); si no→ACTIVAR PARA TRABAJAR (verde, pide permiso). actualizarPanelEstado() pinta botón+cartel según estado y limpia labelEstadoCaptura (se arregló el mensaje contradictorio "No diste el permiso" que quedaba pegado). Probado en celu real: prende y apaga bien, sin mensajes contradictorios. Ahora Fernando puede apagar la app al terminar de trabajar (ahorra batería).
- 2026-08-25: 3 TEMAS/PEDIDOS de Fernando (dueño-chofer) a tener en cuenta:
  * (T1 - batería) La app queda siempre activa (ayer no la apagó y seguía corriendo). Consumo actual NO optimizado: captura cada 1s SIEMPRE que Uber está en foco, incluso en el mapa vacío sin viaje. MEJORA a hacer: capturar seguido solo cuando puede haber viaje; en mapa vacío casi no hacer nada (bajar frecuencia / detectar señal de "hay tarjeta" antes de gastar OCR). Esto baja mucho el consumo.
  * (T2 - conflictos / multi-app) Confirmar/tranquilizar: la app YA está filtrada a com.ubercab.driver → si entra un WhatsApp o se usa otra app, Ruta Clara NO hace nada (sin conflicto). PENDIENTE futuro: soportar DiDi y Cabify — mismo método que Uber (capturar tarjeta real de cada una, ver formato de tarifa/km/tiempo, ajustar InterpreteViaje). No empezado.
  * (T3 - posición configurable) ✅ HECHO Y VALIDADO 25/08. En la config hay un selector "¿Dónde querés ver el semáforo?" con 4 opciones: Arriba centro / Arriba izq / Arriba der / Abajo (tablet). Se guarda en SharedPreferences (KEY_POSICION = "arriba_centro"|"arriba_izq"|"arriba_der"|"abajo") y mostrarSemaforo() la usa para params.gravity (margen proporcional a pantalla). Probado en celu real: se movió a arriba-derecha correctamente. Cálculo seguía OK (ARS8.702/31min → $16.842/h 🟢, 8702*60/31=16842 ✓).
  * (T4) Fernando mencionó un 4º tema que no recordaba — preguntarle cuando lo recuerde.
- 2026-08-25: ✅ MATERIAL DE MARKETING creado. Flyer promocional (1080x1350, para WhatsApp/grupos de choferes) e ícono de app (1024x1024, semáforo estilizado). Guardados en ~/proyectos/chofer-rentable/marketing/ (flyer_rutaclara.png, icono_rutaclara.png). Contenido del flyer: "Sabé al instante si un viaje te conviene", semáforo CONVIENE/OJO/NO CONVIENE, "Pronto también para DiDi y Cabify", "Sumate y ayudá a mejorarla", WhatsApp 11 2887-0411, "Gratis primeros 50". Estilo: negro tablero + verde/ámbar/rojo, fuentes Bricolage Grotesque + Outfit + Geist Mono. WhatsApp de Fernando: 11 2887-0411 (para futuros materiales).
- 2026-08-25: ✅ ÍCONO DE LA APP REEMPLAZADO Y VALIDADO. Se cambió el robotito verde por el semáforo estilizado. PNGs generados en los 5 mipmap (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi) como ic_launcher.png + ic_launcher_round.png + ic_launcher_foreground.png. El ícono adaptativo (mipmap-anydpi-v26/ic_launcher.xml y ic_launcher_round.xml) ahora usa <background android:drawable="@color/ic_launcher_bg"> (#0A0D10, definido en values/ic_launcher_bg_color.xml) + <foreground android:drawable="@mipmap/ic_launcher_foreground">. OJO/BUG RESUELTO: había ic_launcher.webp viejos (el robot) que chocaban con los .png nuevos → "Duplicate resources". Fernando los borró con `rm res/mipmap-*/ic_launcher.webp res/mipmap-*/ic_launcher_round.webp`. El puente Cowork NO puede borrar archivos → los rm los corre Fernando. Confirmado visualmente en el celu: se ve el semáforo.
- 2026-08-25: ✅ GUÍA DE INSTALACIÓN creada (marketing/guia_instalacion.png, 1080x1620). 6 pasos simples para que un amigo con poco conocimiento instale el APK solo (descargar, instalar, "Analizar app" de Xiaomi, activar, configurar objetivo, listo) + WhatsApp de Fernando para dudas.
- 2026-08-25: DECISIÓN DE DISTRIBUCIÓN. Para los primeros amigos: pasar el APK (rutaclara.apk) por WhatsApp JUNTO con la guia_instalacion.png. Cuando pase de 10-15 personas: armar Firebase App Distribution (gratis, actualizaciones automáticas). Play Store (US$25 pago único) más adelante, con CUIDADO: apps con Accessibility Service que leen otras apps son revisadas con lupa por Google y pueden ser rechazadas — planear bien la presentación. El APK actual es DEBUG; para repartir en serio conviene generar un APK release firmado (pendiente).
- 2026-08-25: PLAN acordado con Fernando: PRIMERO repartir a amigos de confianza (flyer + mensaje + APK + guía), y cuando esté BIEN PROBADA, ir a Play Store para que sea confiable para cualquiera. Preocupación de Fernando: el cuadradito gris del APK en WhatsApp genera desconfianza — se le explicó que es de WhatsApp y no se puede cambiar; se resuelve con flyer + mensaje personal de Fernando. mensaje_whatsapp.txt tiene el texto de presentación listo para copiar/pegar.
- 2026-08-25: PENDIENTES para próximas sesiones (prioridad): (1) MAPA de zonas de seguridad — el diferencial PAGO, gran próximo hito; (2) generar APK release firmado (keystore) para repartir versión estable; (3) optimizar batería (no capturar cuando no hay viaje / mapa vacío); (4) soportar DiDi y Cabify (mismo método OCR que Uber, capturar tarjeta real de cada una); (5) robustecer parser del destino (OCR desordena líneas); (6) limpiar Log.d de diagnóstico; (7) Firebase App Distribution cuando crezca el grupo; (8) Fernando tiene un 4º tema/idea que no recordaba — preguntarle.
- 2026-08-25: PRÓXIMOS PASOS: (a) compilar/instalar la UI nueva de "arranque fácil"; (b) confirmar visualmente el fix del semáforo pegado con 2 viajes seguidos; (c) optimizar batería (T1); (d) posición del semáforo configurable (T3); (e) probar en la TABLET; (f) soportar DiDi/Cabify (T2); (g) robustecer parser del destino; (h) limpiar Log.d de diagnóstico; (i) MAPA de zonas (diferencial pago); (j) repartir a los primeros 50 amigos.
- 2026-08-25: BUG CRÍTICO REPRODUCIDO Y DIAGNOSTICADO con Logcat + capturas reales del Redmi. Para el mismo viaje de ARS 7,784 / 23 min / 15.6 km, el parser llegó a combinar los datos del viaje con otros importes visibles: +ARS 950 del mapa, ARS 0.00 del saldo, una lectura cortada "ARS 7" y ARS 9,700 de una promoción. Por eso mostró varios semáforos contradictorios. La causa era que `InterpreteViaje` tomaba el primer texto con "ARS" de toda la pantalla.
- 2026-08-25: CORRECCIÓN IMPLEMENTADA Y PROBADA, todavía pendiente de instalar/validar en dispositivos. `InterpreteViaje` ahora agrupa cada tarifa con su propio texto "Viaje:" usando posición, elige la tarjeta activa de adelante (prioriza la que tiene "Aceptar" y la de mayor escala), descarta tarifas cortadas y no mezcla promociones/saldo/adicionales. El servicio actualiza inmediatamente si cambia tarifa/km/min, tolera una lectura vacía y no repite la misma oferta. Se agregaron 4 pruebas unitarias basadas en el caso real; `testDebugUnitTest` y `assembleDebug` pasan.
- 2026-08-25: APK CORREGIDO INSTALADO en el Redmi Note 8 Pro por ADB (`install -r`, conservó configuración). Se verificó que el paquete está actualizado y que `RutaClaraAccessibilityService` sigue habilitado. La reinstalación cortó MediaProjection, por lo que Fernando debe abrir Ruta Clara y tocar una vez "ACTIVAR PARA TRABAJAR" antes de la prueba. PENDIENTE INMEDIATO: validar con solicitudes reales que tome solo la tarjeta de adelante, cambie al aparecer otro importe y no repita colores contradictorios.
- 2026-08-25: ✅ CORRECCIÓN DE TARIFA/TARJETA VALIDADA CON 3 SOLICITUDES REALES. (1) ARS6,402 / 21 min / 11.3 km → $18,291/h VERDE (6402×60/21=18,291 ✓); (2) la tarjeta de adelante cambió a ARS6,353 con mismos 21 min / 11.3 km → el semáforo se actualizó a $18,151/h VERDE (6353×60/21=18,151 ✓); (3) ARS4,820 / 20 min / 9.3 km → $14,460/h ÁMBAR/OJO (4820×60/20=14,460 ✓, objetivo $15,000/h). No mezcló el adicional del mapa, saldo ni promociones. Fernando notó que el semáforo se va rápido, pero considera correcto/aceptable por ahora porque permite que una oferta nueva lo actualice; no cambiar duración todavía.
- 2026-08-25: DECISIÓN DE UX posterior a la prueba: eliminar la duración fija de 8 segundos. Comportamiento deseado: el semáforo permanece mientras la tarjeta activa siga visible, cambia inmediatamente si Uber pone otra oferta adelante y desaparece cuando la tarjeta se va. Implementación local: cada lectura válida de la misma oferta renueva una vigencia de 3s sin redibujar; una oferta diferente reemplaza el cartel; sin confirmaciones o tras 2 lecturas vacías se cierra por seguridad. ✅ Pruebas unitarias y `assembleDebug` pasan. PENDIENTE: cuando vuelva Fernando con el Redmi, instalar/validar este ajuste.
- 2026-08-25: APK CON DURACIÓN DINÁMICA INSTALADO en el Redmi por ADB (`192.168.0.18:40031`, `install -r` exitoso). scrcpy quedó abierto y se lanzó MainActivity. PENDIENTE: Fernando debe tocar "ACTIVAR PARA TRABAJAR" y validar con una oferta real que el semáforo permanezca mientras la tarjeta siga visible, cambie con la siguiente y desaparezca al retirarse.
- 2026-08-25: ✅ DURACIÓN DINÁMICA VALIDADA CON UBER REAL. Oferta ARS3,400 / 13 min / 2.3 km → $15,692/h VERDE (3400×60/13=15,692 ✓). Logcat: se mostró a las 11:58:56, la misma oferta se confirmó repetidamente sin redibujar ni cambiar valores hasta las 11:59:11, y al desaparecer la tarjeta se borró a las 11:59:13. El semáforo permaneció ~17s, lo que duró la oferta, en lugar de cerrarse a los 8s. Después se apagó/desconectó la depuración inalámbrica del Redmi: eso solo corta ADB/scrcpy y NO afecta el funcionamiento autónomo de Ruta Clara.
- 2026-08-25: ✅ SUITE DE REGRESIÓN AMPLIADA a 9 pruebas unitarias (`InterpreteViajeTest`), todas pasan con 0 fallas/errores. Además de los 4 casos anteriores, ahora cubre: pantalla real contaminada con saldo + promoción + miniatura, cambio inmediato ARS6,402→ARS6,353 en la tarjeta activa, cálculo y colores verde/ámbar/rojo por hora, límites exactos 90%/100% y cálculo por kilómetro. `assembleDebug` también pasa. No cambió la lógica productiva en este paso; se reforzó la protección contra regresiones.
- 2026-08-25: PRUEBA EN TABLET INICIADA. Samsung Galaxy Tab A7 Lite SM-T220 vinculada por ADB (`192.168.0.41:32887`), última APK instalada con `install -r` exitoso, scrcpy abierto y MainActivity lanzada. PENDIENTE INMEDIATO: activar captura y servicio de accesibilidad en la tablet, configurar objetivo/posición, abrir Uber Driver y validar lectura de la tarjeta, cálculo, duración dinámica y tamaño/posición del semáforo en pantalla grande.
- 2026-08-25: TABLET LISTA PARA PRUEBA REAL. En la primera tarjeta (ARS3,456 / 16 min / 5.8 km) no apareció el semáforo; diagnóstico por ADB: MediaProjection estaba activo a 1340×800 pero `enabled_accessibility_services=null`. Fernando activó manualmente RutaClara en Ajustes → Accesibilidad. Verificación posterior: `RutaClaraAccessibilityService` y `ScreenCaptureService` están ambos activos. Configuración confirmada: objetivo $15,000/h, criterio hora, posición arriba-centro. PENDIENTE: esperar otra solicitud real y validar OCR/cálculo/posición en pantalla dividida.
- 2026-08-25: ✅ PRIMERA SOLICITUD REAL VALIDADA EN TABLET/PANTALLA DIVIDIDA. ARS8,710 / 26 min / 18.4 km → $20,100/h VERDE (8710×60/26=20,100 ✓). El semáforo apareció arriba-centro, con tamaño legible y sin tapar el botón Aceptar. Fernando percibió cierta demora; logs muestran que en la tablet el ciclo captura+OCR de la pantalla completa 1340×800 puede tardar aproximadamente 1–2s (y una captura inicial anómala tardó más). Se dejó monitor de `RutaClaraService` abierto para medir la siguiente oferta antes de optimizar. Posible mejora posterior: dejar de guardar/comprimir un PNG en cada captura de producción y reutilizar el reconocedor OCR para reducir latencia, batería y almacenamiento.
- 2026-08-25: ✅ DEMORA EN TABLET MEDIDA Y OPTIMIZADA. Los logs confirmaron variación real: ARS3,400/15min se resolvió en ~1.2s con una lectura, mientras algunas ofertas necesitaron dos lecturas y rondaron ~3s. Causa evitable encontrada en `ScreenCaptureService`: cada ciclo comprimía/guardaba un PNG 1340×800 antes del OCR, creaba un reconocedor ML Kit nuevo y escribía cada línea OCR en Logcat. Se eliminó el guardado automático de capturas, ahora se reutiliza un único reconocedor (se cierra con el servicio) y el log queda resumido. No se tocó parser, fórmula ni colores. Validaciones de las capturas: ARS11,393/40min → $17,089/h VERDE; ARS3,400/15min → $13,600/h ÁMBAR. Las 9 pruebas unitarias y `assembleDebug` pasan. APK optimizado instalado por ADB en la Galaxy Tab A7 Lite a las 12:32; accesibilidad quedó activada y Ruta Clara abierta. Fernando debe tocar una vez “ACTIVAR PARA TRABAJAR” (la reinstalación corta MediaProjection) y medir la próxima oferta real.
- 2026-08-25: ✅ SEGUNDA CORRECCIÓN DE LATENCIA/PERMANENCIA INSTALADA EN TABLET. Fernando confirmó que todavía aparecía tarde y se iba rápido. Logcat mostró la causa precisa: si Uber cambiaba la tarjeta durante un OCR, esos eventos se descartaban por `capturaEnCurso`; después de una lectura válida Uber podía quedar ~10s sin eventos y la vigencia de 3s borraba el semáforo aunque la tarjeta siguiera visible. `RutaClaraAccessibilityService` ahora conserva una captura pendiente y la ejecuta apenas termina el OCR actual, arranca 80ms después del primer evento, y mientras haya semáforo hace seguimiento propio cada 700ms aunque Uber no emita eventos. La vigencia de seguridad subió a 6s; al desaparecer la tarjeta, 2 lecturas vacías siguen apagando el cartel. Se evita duplicar capturas programadas. Fórmula/parser sin cambios. Las 9 pruebas de `InterpreteViaje` y `assembleDebug` pasan; APK instalado con éxito y accesibilidad conservada. Ruta Clara quedó abierta: falta tocar “ACTIVAR PARA TRABAJAR” y validar con la próxima oferta real velocidad, actualización y duración.
- 2026-08-25: ✅ SEGUNDA CORRECCIÓN VALIDADA EN TABLET CON OFERTA REAL. Captura enviada: ARS3,400 / Viaje 8 min (2.4 km) → $25,500/h VERDE, cálculo correcto. Medición de Logcat en la secuencia de la oferta: primer evento a las 12:44:52.600 y primera evaluación válida a las 12:44:54.218 (~1.6s, incluyendo una primera lectura antes de que la tarjeta terminara de dibujarse). Después la oferta se confirmó automáticamente cada ~1.3–1.5s durante unos 17s, aunque Uber quedó sin emitir eventos; el cartel ya no venció a los 3s. Cuando la tarjeta se retiró, dos lecturas vacías la borraron a las 12:45:11.038. Quedó validada la permanencia dinámica; latencia actual real en esta tablet ronda 1.0–1.6s según si alcanza una o dos lecturas OCR.
- 2026-08-25: DECISIÓN DE PRUEBA SIN DEMANDA. Existe el proyecto separado `~/proyectos/chofer-rentable/MockUber` (app `com.tanomanfer.mockuber`), actualmente muy básico y con formato viejo. Próximo paso propuesto: convertirlo en simulador de alta demanda con tarjetas visualmente iguales a las capturas reales, secuencia automática de 20–30 ofertas, cambios rápidos, tarjeta Exclusivo, Radar con varias opciones escalonadas, desaparición y reemplazo. Probar primero en la Galaxy Tab A7 Lite real —mejor que el emulador porque valida OCR, overlay, rendimiento y resolución 1340×800 reales— y usar emulador además para repetición automática. Esto no toca ni automatiza Uber real.
- 2026-08-25: FLUJO REAL DE TABLET/MAPA ACLARADO POR FERNANDO. Trabaja en horizontal y pantalla dividida: Uber Driver ocupa el panel izquierdo; Waze ocupa el derecho. Al aceptar/tomar un viaje, Uber lo redirige a Waze para navegar. Para el futuro diferencial de zonas, Fernando imagina un mapa propio de Ruta Clara basado en Google Maps, configurable con lugares seguros/inseguros y los datos de zonas detallados en el proyecto. Base ya existente en el prototipo web: `/trip-tablet` y `/zonas` usan Leaflet/OpenStreetMap, muestran destino y zonas cercanas con círculo de 500m; motivos `inseguridad`, `calle_rota`, `otro`, nota opcional, zonas propias o compartidas, respaldadas por tabla Supabase `zonas`. PENDIENTE DE DISEÑO: decidir cómo convivirá el mapa propio de Ruta Clara con Waze en el panel derecho y si Ruta Clara solo mostrará riesgo antes de aceptar o también incorporará navegación; no asumir que se puede dibujar dentro de Waze/Google Maps externos.
- 2026-08-25: PRIORIDAD INMEDIATA CONFIRMADA POR FERNANDO. Pausar por ahora simulador de alta demanda y mapa de zonas. Primero Fernando saldrá a trabajar con la Galaxy Tab A7 Lite y probará Ruta Clara durante un turno real; después se corregirá cualquier falla observada y recién entonces se preparará el APK estable/instalación para los amigos. La tablet tiene la versión más nueva con parser corregido, OCR optimizado y seguimiento dinámico. IMPORTANTE: no repartir todavía `~/Escritorio/RutaClara.apk`, porque esa copia es anterior a los últimos arreglos; al cerrar la prueba de calle hay que generar/copiar un APK nuevo claramente versionado y actualizar la guía si hiciera falta.
- 2026-08-25 (Cowork/Claude): ESTRATEGIA GO-TO-MARKET DEFINIDA POR FERNANDO (freemium). Secuencia: (1) SEMÁFORO GRATIS → que la gente lo use (ya está listo, se prueba mañana miércoles en la calle con la Galaxy Tab). (2) LANDING + META (Facebook/Instagram ads) → captar choferes, que se anoten y bajen el semáforo gratis. (3) GANCHO "SE VIENE EL MAPA" → prometer el mapa de zonas como el agregado que diferencia a Ruta Clara del resto de las apps. (4) MAPA DE ZONAS → construir EN PARALELO, listo para cuando el semáforo esté bien probado; se cobra (parte paga). Fernando quiere avanzar el mapa sin perjudicar lo hecho.
- 2026-08-25 (Cowork/Claude): CONFIRMADO — el mapa NO rompe el semáforo. Son separados: semáforo = app Android (Kotlin, ruta-clara-dashboard/app), mapa = web + Supabase (Leaflet, pantallas /zonas y /trip-tablet, tabla `zonas`). Trabajar el mapa no toca el código del semáforo. Se puede construir en paralelo sin riesgo.
- 2026-08-25 (Cowork/Claude): RECORDATORIO — la LANDING YA EXISTE Y FUNCIONA (ruta-clara-dashboard.vercel.app: capta lead nombre+email → tabla `leads` Supabase → redirige a /bienvenida). NO hacerla de cero. Para la estrategia hay que MEJORARLA: (a) poner el gancho "próximamente el mapa de zonas" bien visible; (b) agregar el Pixel de Meta en /bienvenida para medir conversiones (estaba pendiente); (c) que sea el lugar donde la gente baja el semáforo gratis. También pendiente de antes: mail de bienvenida con Resend en n8n (self-hosted en n8n-n8n.fihebt.easypanel.host). Credenciales Supabase correctas del proyecto chofer-rentable: URL https://reptacnrguavhxtwhldl.supabase.co (env vars en Vercel son "Sensitive").
- 2026-08-25 (Cowork/Claude): PENDIENTE que Fernando mencionó — quiere que la app "prometa un agregado que la diferencia" (el mapa). Ese mensaje va en la landing Y en la propia app (ej: una sección "Próximamente: mapa de zonas de seguridad").
- 2026-08-25 (Cowork/Claude): ⭐ VISIÓN COMPLETA DEL MAPA DE ZONAS (definida por Fernando, es EL diferenciador pago). Cuando entra un viaje de Uber, junto al semáforo aparece un MINI-MAPA del destino. Requisitos: (1) visible en celus de 6" para arriba, mejor en tablet; (2) al TOCARLO se abre/agranda (con ZOOM, para acercarse y ver a dónde va el viaje) SIN tapar el botón Aceptar; (3) muestra las ZONAS que el propio chofer configuró, colores: VERDE=zona tranquila/segura, AMARILLO=calle rota o no le gusta pasar, ROJO=peligroso, SIN COLOR=no pasó por ahí / lugar nuevo (sin info); (4) el chofer configura zonas con el tiempo. Flujo ideal: semáforo verde + zona verde → acepta directo; si no conoce → toca, amplía, zoom, decide. VALOR ÚNICO: ninguna app da las dos cosas juntas (cuánto pagás + a dónde vas/si es seguro) antes de aceptar.
- 2026-08-25 (Cowork/Claude): DESAFÍO TÉCNICO del mapa (es lo más complejo, VARIAS SESIONES): (a) el overlay flotante Android hoy es vista simple (círculo+texto vía WindowManager); meter mapa interactivo con zoom encima de Uber = salto grande, camino probable = WebView en el overlay cargando un mapa Leaflet liviano (reutiliza lo web ya hecho). (b) GEOCODING: el OCR da el destino como TEXTO; hay que convertirlo a lat/lng para ubicarlo y cruzarlo con las zonas (geocoder Google/Nominatim). (c) BASE REUTILIZABLE en web: zonas.tsx (configurar zonas, tabla Supabase `zonas`), trip-mobile.tsx y trip-tablet.tsx (Leaflet/OSM + zonas cercanas círculo 500m). NO empezar de cero. PLAN: primero MAQUETA VISUAL para lockear diseño con Fernando, después construir por fases. NO bloquea la prueba de calle de mañana (paralelo, no toca el semáforo).
- 2026-08-25 (Cowork/Claude): ✅ MAQUETA VISUAL del mapa hecha y aprobada por Fernando (guardada en marketing/maqueta_mapa_zonas.png). Muestra: (vista 1) semáforo + mini-mapa en esquina con "TOCÁ PARA VER" + tarjeta Uber con Aceptar; (vista 2) mapa agrandado con zoom +/−, zonas de colores (SEGURA verde / CALLE ROTA amarillo / PELIGRO rojo), pin "Tu destino", leyenda y botón cerrar.
- 2026-08-25 (Cowork/Claude): ⭐ DISEÑO RESPONSIVE DEFINITIVO (aclarado por Fernando, CLAVE para construir): el mapa se ADAPTA al dispositivo. En CELULAR (pantalla chica): el mini-mapa debe ser chico y en una esquina que NO tape NUNCA el botón Aceptar (perder segundos = perder el viaje; la decisión es en segundos). Recién si el chofer lo TOCA se agranda. En TABLET (Fernando trabaja en horizontal, pantalla dividida: Uber izquierda + Waze derecha): hay espacio de sobra, el mapa de Ruta Clara PUEDE abrirse a la derecha mostrando a dónde va, sin problema — de hecho es cómodo porque al aceptar/navegar Uber redirige a Waze igual. Regla de oro: en celu NUNCA tapar Aceptar; en tablet aprovechar el espacio grande.
- 2026-08-25 (Cowork/Claude): 💰 DECISIÓN DE COSTOS DEL MAPA (importante para el negocio; Fernando NO puede costear caro porque vende a choferes que no les sobra la plata). Se DESCARTÓ Google Maps: retiró el crédito de US$200/mes en marzo 2025; ahora Dynamic Maps US$7/1000 + Geocoding US$5/1000 tras 10.000 gratis por SKU/mes → con 10+ choferes de uso intensivo se va a cientos de US$/mes = inviable. DECISIÓN (Claude, con visto bueno de Fernando de "decidí vos, que sea buena y barata"): usar OpenStreetMap (tiles gratis vía Leaflet) + Nominatim (geocoding gratis), CON CACHE agresivo (cada dirección se geocodifica una sola vez y se guarda) → costo $0 para pocos usuarios. Para ESCALA: self-hostear tiles/Nominatim de Argentina en VPS propio de Fernando (ya tiene HostGator VPS con n8n + easypanel) → costo fijo bajísimo (~US$5-20/mes) sin importar cantidad de usuarios. Nunca atarse a un proveedor caro. Fuentes: woosmap.com (pricing 2026), operations.osmfoundation.org (política de tiles: uso comercial OK con atribución + User-Agent identificable + cache 7 días; prohibido bulk download/prefetch).
- 2026-08-25 (Cowork/Claude): ✅ PIEZA CLAVE DEL MAPA CONSTRUIDA Y PROBADA (SIN tocar el semáforo). Archivo NUEVO: `app/src/main/assets/mapa.html` (se creó la carpeta assets). Es una página Leaflet+OpenStreetMap autocontenida que: (1) recibe el destino por URL `mapa.html?destino=<texto>`; (2) lo geocodifica con Nominatim CON CACHE en localStorage (no reconsulta direcciones ya vistas → gratis/rápido); (3) muestra el mapa centrado en el destino con pin, zoom, y zonas de colores de ejemplo (verde/amarillo/rojo, círculos) + leyenda. Probado por Fernando en el navegador con "Av. Rivadavia 5400, Caballito" → ubicó el pin EXACTO en Primera Junta. ✅ Funciona, costo $0. Esta es la MISMA página que después cargará el WebView del overlay (mini-mapa) y la vista agrandada. Zonas hoy son de ejemplo (hardcodeadas en zonasEjemplo); FALTA conectarlas a la tabla Supabase `zonas`.
- 2026-08-25 (Cowork/Claude): 📋 PLAN DEL MAPA PARA CODEX (Codex es más autónomo para ejecutar: compila/instala/prueba solo en la máquina; Claude/Cowork no ejecuta terminal, corre en la nube y edita archivos por el puente). PENDIENTE, en orden: (Fase C) meter `mapa.html` en un WebView dentro del overlay del semáforo — MINI-MAPA en esquina que NO tape Aceptar en celu; en tablet permitir panel a la derecha. Pasarle el destino que ya lee InterpreteViaje: `webView.loadUrl("file:///android_asset/mapa.html?destino=" + Uri.encode(viaje.destino))`. (Fase D) al TOCAR el mini-mapa, abrir la vista agrandada con zoom (misma mapa.html a pantalla más grande). ⚠️ ESTO TOCA RutaClaraAccessibilityService (el overlay) → hacerlo DESPUÉS de la prueba de calle de mañana para no arriesgar el semáforo. (Fase A/B posterior) conectar las zonas reales desde Supabase a mapa.html y terminar zonas.tsx para configurarlas. Permiso Android necesario para WebView con internet: INTERNET (ya casi seguro está por ML Kit, verificar en AndroidManifest).
- 2026-08-26 (Codex): ✅ BOTÓN/PANTALLA DE PRUEBA DEL MAPA IMPLEMENTADOS E INSTALADOS sin tocar `RutaClaraAccessibilityService.kt`, el semáforo, el parser ni `InterpreteViaje`. Se agregó “Ver mapa (prueba)” al final de la configuración; abre `MapaPruebaActivity`, un WebView a pantalla completa con JavaScript y DOM Storage habilitados, que carga `file:///android_asset/mapa.html` con el destino “Av. Rivadavia 5400, Caballito, Buenos Aires”. También se agregó el permiso `INTERNET` y se registró la Activity en el manifiesto. `assembleDebug` terminó con `BUILD SUCCESSFUL`. El Redmi Note 8 Pro se conectó por ADB Wi‑Fi en `192.168.0.18:33427`; como tenía solo 545 MB libres, se eliminó únicamente el instalador viejo `/sdcard/Download/rutaclara.apk` y Android redujo cachés temporales, dejando 1 GB libre. `adb install -r` terminó con `Success` (última actualización 2026-08-26 09:39:33) y `MainActivity` quedó abierta y activa. IMPORTANTE: la reinstalación corta el permiso temporal de MediaProjection; para usar el semáforo otra vez hay que tocar “ACTIVAR PARA TRABAJAR”.
- 2026-08-26 (Fernando/Codex): ✅ PRUEBA VISUAL DEL MAPA VALIDADA EN EL REDMI NOTE 8 PRO mediante scrcpy. El botón naranja “VER MAPA (PRUEBA)” aparece correctamente al final de la configuración. Al tocarlo se abre el WebView a pantalla completa y `mapa.html` carga correctamente por internet: el destino “Av. Rivadavia 5400, Caballito, Buenos Aires” queda ubicado en Primera Junta, con pin, popup, controles de zoom, zonas de ejemplo verde/amarilla/roja, leyenda y atribución de Leaflet/OpenStreetMap visibles. Queda confirmada la base Android+WebView para continuar más adelante con el mini-mapa/overlay y las zonas reales.
- 2026-08-26 (Codex): ✅ MAPA CON ÚLTIMO DESTINO REAL IMPLEMENTADO. Sin cambiar cálculo, parser, `InterpreteViaje` ni vista/lógica del semáforo, `RutaClaraAccessibilityService` ahora guarda en `SharedPreferences` (`ruta_clara_config`, clave `ultimo_destino`) cualquier destino no vacío de un viaje válido. `MapaPruebaActivity` lee esa clave y carga `mapa.html` con el destino codificado; si todavía no existe, mantiene el ejemplo “Av. Rivadavia 5400, Caballito, Buenos Aires”. Las 9 pruebas de `InterpreteViajeTest` pasan (`0` fallas, `0` errores) y `assembleDebug` terminó con `BUILD SUCCESSFUL`. APK instalado en el Redmi Note 8 Pro mediante ADB Wi‑Fi (`192.168.0.18:43349`) con `Success`; última actualización 2026-08-26 09:51:37 y `MainActivity` quedó iniciada. IMPORTANTE: como toda reinstalación, corta MediaProjection; hay que tocar “ACTIVAR PARA TRABAJAR” antes de capturar el próximo viaje real.
- 2026-08-26 (Codex): ✅ PRIMER MINI-MAPA FLOTANTE IMPLEMENTADO E INSTALADO. El overlay existente ahora muestra, a la derecha del cartel del semáforo, un WebView compacto de `120x80dp` con `mapa.html` centrado en el destino de la oferta válida. El mini-mapa es solo visual/no táctil, se oculta si el viaje no trae destino y se destruye/desaparece junto con el semáforo; no se cambió cálculo, parser, colores ni evaluación. Para el tamaño pequeño, `mapa.html` oculta cartel, leyenda, popup y zoom, dejando mapa+pin para lectura rápida; la pantalla completa de prueba conserva todos sus controles. Las 9 pruebas de `InterpreteViajeTest` pasan (`0` fallas, `0` errores) y `assembleDebug` terminó con `BUILD SUCCESSFUL`. APK instalado en el Redmi Note 8 Pro con `adb install -r` (`Success`, última actualización 2026-08-26 10:13:30) y Ruta Clara quedó abierta. PENDIENTE INMEDIATO: tocar “ACTIVAR PARA TRABAJAR” porque la reinstalación corta MediaProjection y validar con una oferta real que semáforo+mini-mapa aparezcan juntos, no tapen “Viaje disponible” y desaparezcan al retirarse la tarjeta.
- 2026-08-26 (Fernando/Codex): ✅ MINI-MAPA VALIDADO SOBRE UNA OFERTA REAL EN EL REDMI. Oferta UberX Exclusivo ARS3,400 / 9 min / 3.1 km → `$22666/hora`, VERDE/CONVIENE (cálculo correcto). El mini-mapa apareció a la derecha del semáforo con el pin visible, en formato compacto, y el overlay no tapó la tarjeta ni el botón “Aceptar”. Destino visible en Uber: “Lola Mora 285, Ituzaingo”. Queda confirmada la integración básica semáforo+mapa antes de aceptar. `scrcpy` volvió a desconectarse por el cambio/corte habitual de ADB Wi‑Fi y fue reconectado automáticamente en el nuevo puerto `192.168.0.18:41629`.
- 2026-08-26 (Fernando): ⚠️ REGLA OBLIGATORIA PARA LA PRÓXIMA FASE INTERACTIVA DEL MAPA. El chofer debe poder tocar el mini-mapa, ampliarlo, moverlo y usar zoom mientras la oferta de Uber continúa visible. El mapa ampliado debe permanecer como overlay sobre Uber y NUNCA abrir una Activity/pantalla completa que oculte la oferta. La zona del botón “Aceptar” debe quedar siempre libre y operable: consultar el mapa jamás puede impedir ni demorar la aceptación del viaje. Debe existir una forma rápida de minimizar/cerrar el mapa y volver al tamaño compacto. El mini-mapa actual es deliberadamente no táctil; esta interacción todavía no está implementada.
- 2026-08-26 (Fernando): ⚠️ REGLA OBLIGATORIA SOBRE QUÉ UBICACIÓN MUESTRA EL MAPA. El mapa debe centrarse en el DESTINO FINAL donde hay que llevar al pasajero, nunca en la ubicación actual del chofer ni en el punto de recogida. En una tarjeta Uber, la dirección que aparece después de “A X min” corresponde a buscar al pasajero; la dirección que aparece después de “Viaje: X min” es el destino que Ruta Clara debe extraer, guardar, geocodificar y mostrar. Ejemplo real validado: recogida “Intendente Pérez Quintana, Ituzaingo”; destino correcto “Lola Mora 285, Ituzaingo”. El parser actual llegó a guardar solo “Ituzaingo”, por lo que esta identificación precisa del bloque de destino queda pendiente antes de considerar confiable el mapa.
- 2026-08-26 (Fernando): ✅ DECISIÓN DE ALCANCE DEL MAPA PARA EL MVP. Mostrar solamente el DESTINO FINAL de la oferta; no intentar mostrar a la vez destino y punto de recogida porque en el mini-mapa del celular quedaría demasiado pequeño/confuso. El destino tiene prioridad porque define dónde terminará el viaje y permite evaluar la zona antes de aceptar. Para una versión posterior queda como opción configurable permitir que el chofer elija “destino”, “recogida” o eventualmente “ambos”; no agregar esa complejidad ahora.
- 2026-08-26 (Codex): ✅ EXTRACCIÓN MULTILÍNEA DEL DESTINO FINAL CORREGIDA. Se modificó únicamente el bloque que construye `destino` dentro de `InterpreteViaje.interpretar()`: ahora toma hasta 3 líneas consecutivas debajo de `Viaje:`, alineadas con esa línea, las une con espacios y se detiene ante “Aceptar” o texto claramente ajeno a una dirección. Nunca toma líneas de recogida ubicadas arriba de `Viaje:`. Caso real cubierto: recogida “Intendente Pérez Quintana, Ituzaingo” + destino partido “Lola Mora 285,” / “Ituzaingo” → resultado exacto “Lola Mora 285, Ituzaingo”. Se agregaron 2 pruebas unitarias; `InterpreteViajeTest` ahora tiene 11 pruebas, todas con `0` fallas y `0` errores. No se modificaron tarifa, km, minutos, matching de tarjeta, `evaluar`, colores ni overlay. `testDebugUnitTest` y `assembleDebug` terminaron con `BUILD SUCCESSFUL`. APK instalado en el Redmi Note 8 Pro mediante `adb install -r` (`Success`, última actualización 2026-08-26 10:33:58) y `MainActivity` quedó abierta. PENDIENTE: tocar “ACTIVAR PARA TRABAJAR” porque la reinstalación corta MediaProjection y validar con la próxima oferta real que el mini-mapa se centre en la dirección completa del destino final.
- 2026-08-26 (Fernando/Codex): ✅ DESTINO MULTILÍNEA VALIDADO CON OFERTA REAL. Tarjeta: ARS3,400 / Viaje 16 min (5.3 km), con recogida de tres líneas arriba de `Viaje:` (“Los Matreros 2849, Buenos Aires, Ituzaingó 1714, Argentina, Ituzaingo”) y destino de dos líneas debajo (“Roque Sáenz Peña, San” / “Antonio De Padua, Merlo”). Ruta Clara guardó exactamente `Roque Sáenz Peña, San Antonio De Padua, Merlo`, sin mezclar la recogida. El semáforo calculó correctamente `$12750/hora` ROJO/NO CONVIENE y el mini-mapa apareció con pin. La corrección queda validada en uso real.
- 2026-08-26 (Fernando): 📌 PRÓXIMAS FASES DEL MAPA, EN ESTE ORDEN. (1) Hacer que el mini-mapa del overlay sea táctil: al tocarlo debe ampliarse sin salir de Uber, permitir arrastrar el mapa y usar zoom/pinch con dos dedos para inspeccionar bien la zona. En celular debe reservar siempre libre y operable el botón “Aceptar”; en tablet debe aprovechar el espacio extra. Debe incluir una forma rápida de minimizar y volver al tamaño compacto. (2) Recién después, permitir que el chofer marque desde el mapa zonas propias como peligrosa/roja o calle rota/amarilla (y posteriormente segura/verde), para construir su información con el uso. Fernando pausó el trabajo temporalmente antes de comenzar la fase interactiva.
- 2026-08-26 (Codex): ✅ FASE INTERACTIVA DEL MAPA IMPLEMENTADA EN CÓDIGO. El mini-mapa compacto ahora tiene una capa táctil: un toque lo amplía dentro del mismo overlay, sin abrir una Activity ni salir de Uber. En modo ampliado Leaflet recibe directamente los gestos para arrastrar y hacer pinch/zoom con dos dedos; un botón naranja “−” lo devuelve al tamaño compacto. El tamaño ampliado se calcula de forma responsive (máximo `320dp` de ancho y `300dp` de alto, limitado también al 34% de la altura real) y siempre se posiciona arriba-centro para reservar la zona inferior del botón “Aceptar”, incluso si el semáforo compacto estaba configurado abajo. Solo se quitó `FLAG_NOT_TOUCHABLE` para habilitar interacción; se mantiene `FLAG_NOT_FOCUSABLE`. No se modificaron parser, tarifa, km, minutos, evaluación ni colores. Las 11 pruebas de `InterpreteViajeTest` pasan (`0` fallas, `0` errores) y `testDebugUnitTest` + `assembleDebug` terminaron con `BUILD SUCCESSFUL`. PENDIENTE INSTALAR/VALIDAR: el Redmi dejó de publicar el detalle de su servicio ADB y rechazó los puertos anteriores `41629`, `41981` y `43349`; hace falta abrir Depuración inalámbrica y obtener el nuevo “Dirección IP y puerto”.
- 2026-08-26 (Codex): ✅ APK DEL MAPA INTERACTIVO INSTALADO EN EL REDMI. Se volvió a emparejar/conectar el Redmi Note 8 Pro por ADB Wi‑Fi (`192.168.0.18:41329`), `adb install -r` terminó con `Success` (última actualización 2026-08-26 11:07:47) y `MainActivity` quedó abierta. PENDIENTE INMEDIATO: tocar “ACTIVAR PARA TRABAJAR” porque la reinstalación corta MediaProjection; con la próxima oferta real validar toque para ampliar, arrastre, pinch/zoom con dos dedos, botón “−” para minimizar y que “Aceptar” permanezca visible y operable.
- 2026-08-26 (Fernando/Codex): 🛠️ PRIMERA OFERTA CON VERSIÓN INTERACTIVA DETECTÓ UN PROBLEMA RESPONSIVE. Oferta ARS6,316 / 19 min / 10.8 km → `$19945/hora` VERDE/CONVIENE; destino guardado correctamente como “Int. Dastugue 1781, Paso Del Rey, Moreno”. El mini-mapa no se veía porque semáforo+mapa en fila excedían el ancho útil del celular y Android recortaba el WebView fuera de pantalla. Corrección implementada: en modo compacto el mapa ahora queda debajo del semáforo, centrado y con tamaño `180x90dp`; al minimizar vuelve a ese formato. El modo ampliado, arrastre, pinch/zoom y reserva del botón “Aceptar” no cambiaron. Las 11 pruebas pasan y `assembleDebug` terminó con `BUILD SUCCESSFUL`. APK corregido instalado con `Success` (última actualización 2026-08-26 11:20:57) y Ruta Clara quedó abierta. PENDIENTE: reactivar captura y validar con otra oferta real que el mapa compacto se vea y pueda ampliarse.
- 2026-08-26 (Fernando/Codex): 🛠️ MAPA AMPLIADO SE CERRABA DEMASIADO RÁPIDO — CAUSA CONFIRMADA Y CORREGIDA. En oferta real ARS6,089 / 35 min / 11.4 km (`$10438/hora`, ROJO/NO CONVIENE), al ampliar el mapa el overlay tapaba parte de la tarjeta durante el OCR; dos lecturas vacías cerraban el overlay a las 11:29:25 y la misma oferta reaparecía al retirarlo a las 11:29:26. Corrección: mientras el mapa está ampliado las lecturas vacías no lo cierran; queda fijado durante 20s, y cada nuevo gesto sobre el mapa renueva ese margen. El chofer puede minimizar con “−” (vuelve al seguimiento automático normal) o cerrar todo deslizando el panel negro del semáforo al menos `80dp` hacia la derecha. Una oferta nueva válida sigue reemplazando la anterior. La zona inferior de “Aceptar” permanece fuera del overlay. No se modificaron parser, cálculo ni colores. Las 11 pruebas pasan y `assembleDebug` terminó con `BUILD SUCCESSFUL`. APK instalado con `Success` (última actualización 2026-08-26 11:31:45), Ruta Clara abierta. PENDIENTE: reactivar captura y validar permanencia, zoom, minimizar, deslizar para cerrar y acceso a “Aceptar” con otra oferta real.
- 2026-08-26 (Fernando/Codex): 🛠️ MINI-MAPA AUSENTE EN ALGUNAS OFERTAS — CAUSA CONFIRMADA Y CORREGIDA. Oferta real ARS3,574 / 16 min / 6.0 km (`$13402/hora`, ROJO/NO CONVIENE), destino “Blas Parera 1370, Ituzaingo”. La primera lectura OCR detectaba tarifa/km/min antes de que Uber terminara de dibujar la dirección, creaba el semáforo con el contenedor del mapa oculto y las lecturas posteriores, aunque ya traían/guardaban el destino, entraban por “misma oferta” sin actualizar el WebView. Corrección: cada confirmación de la misma oferta compara el destino; si aparece o mejora en una lectura posterior, carga/muestra el mini-mapa inmediatamente sin redibujar ni cambiar el semáforo. No usa el último destino viejo como fallback, para evitar mostrar una ubicación equivocada. Las 11 pruebas pasan y `assembleDebug` terminó con `BUILD SUCCESSFUL`. APK instalado con `Success` (última actualización 2026-08-26 11:34:31), Ruta Clara abierta. PENDIENTE: reactivar captura y validar con otra oferta real.
- 2026-08-26 (Fernando/Codex): ✅ MAPA AMPLIADO Y PERMANENCIA VALIDADOS CON OFERTA REAL. El destino apareció en una lectura posterior y Logcat confirmó `Destino completado en lectura posterior → actualizo mini-mapa`; Fernando pudo tocar el mini-mapa y verlo ampliado con el botón naranja “−”. Al desaparecer la tarjeta de Uber, el mapa permaneció abierto sobre la pantalla “Buscando solicitud de viaje”; entre 11:45:55 y al menos 11:46:19 el servicio ignoró correctamente las lecturas vacías mientras el chofer interactuaba, en vez de cerrar y reaparecer como antes. Queda validado el flujo de actualización tardía del destino + ampliación + permanencia para decidir. PENDIENTE MENOR: confirmar explícitamente en otra prueba el gesto de pinch/zoom, minimizar con “−”, deslizar el panel negro hacia la derecha y tocar “Aceptar” con la oferta todavía activa.
- 2026-08-26 (Fernando/Codex): ✅ TARIFA CON BONO `+ARS` CORREGIDA Y VALIDADA EN OFERTA REAL. Se detectó que el OCR podía tomar un bono como `+ARS563 por inicio de...` como tarifa y mostrar una rentabilidad absurda. `InterpreteViaje` ahora descarta siempre importes cuyo `ARS` está precedido por `+`; si todavía no apareció la tarifa real, no muestra semáforo y espera otra lectura. Se agregaron 2 pruebas (bono como único importe y bono cercano junto a tarifa real): las 13 pruebas pasan y `assembleDebug` compila. Validación real posterior: UberX ARS3.539 / 15 min / 4.8 km, destino `Dorrego 32, Merlo, Merlo` → `$14.156/hora`, ÁMBAR/OJO con objetivo `$15.000/h` (3539×60÷15=14156 ✓). Mini-mapa visible y botón `Viaje disponible` libre. La lectura de tarifa vuelve a quedar confiable para pruebas.
