<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/floatingRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_semaforo">

    <LinearLayout
        android:id="@+id/panelSemaforo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Círculo de color tipo semáforo -->
        <View
            android:id="@+id/circuloSemaforo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="14dp"
            android:background="@drawable/circulo_semaforo" />

        <!-- Textos: dato grande + estado chico -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvDatoClave"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="$0/km"
                android:textColor="#FFFFFF"
                android:textSize="24sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvEstado"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="—"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold"
                android:letterSpacing="0.05" />

        </LinearLayout>

    </LinearLayout>

    <FrameLayout
        android:id="@+id/contenedorMapa"
        android:layout_width="180dp"
        android:layout_height="90dp"
        android:layout_marginTop="8dp">

        <WebView
            android:id="@+id/miniMapa"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#232C36"
            android:importantForAccessibility="no"
            android:scrollbars="none" />

        <!-- En modo compacto captura un toque para ampliar. -->
        <View
            android:id="@+id/capaAbrirMapa"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:contentDescription="Ampliar mapa"
            android:focusable="true" />

        <Button
            android:id="@+id/btnMinimizarMapa"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:backgroundTint="#FF6B35"
            android:contentDescription="Minimizar mapa"
            android:text="−"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
